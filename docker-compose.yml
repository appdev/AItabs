services:
  aitabs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aitabs
    restart: unless-stopped
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80"]
      interval: 30s
      timeout: 5s
      retries: 3
